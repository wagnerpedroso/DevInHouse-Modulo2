const objeto1 = {
    umaPropriedade: true,
    duasPropriedade: () => {},
    URLSearchParams: 'ocupado',
    r: 100,
     ${objeto1[key]}
}

for (const key in objeto1) {
    console.log('dentro: ' ${key} eu tenho valor ${objeto1});
}
Object.key(objeto1).forEach(chave => console.log('dento: ') {
    
});